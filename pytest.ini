# pytest.ini
[pytest]
testpaths = tests

markers =
    system: end-to-end system tests for the full CMAPSS RUL pipeline

filterwarnings =
    # Ignore NumPy/Keras DeprecationWarning about the "copy" keyword
    ignore:.*copy keyword.*:DeprecationWarning
